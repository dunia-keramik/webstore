const config = {
  NEXT_PUBLIC_API_HOST: process.env.NEXT_PUBLIC_API_HOST || "http://localhost:5000",
  NEXT_PUBLIC_HOST: process.env.NEXT_PUBLIC_HOST || "dival-bangun.netlify.app",
};

export default config;
